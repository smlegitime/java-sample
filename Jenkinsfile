pipeline {
    agent any
    stages {
        stage ('Initialize') {
            steps {
                echo 'Initializing the code file.'
            }
        }

        stage ('Build') {
            steps {
                echo 'Hello there'
            }
        }

        stage ('Deploy') {
            steps {
                echo 'Deployed an artifact'
            }
        }
    }
}
